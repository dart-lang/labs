name: mobile_test
description: "Used to execute package:unix_api tests on Android and iOS"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  tests:
    path: ../tests
