// AUTO GENERATED FILE, DO NOT EDIT.
// Regenerate with `dart run tool/build_constants.dart`.

// ignore_for_file: non_constant_identifier_names

import 'constant_bindings.g.dart';
int get EACCES {
  final v = get_EACCES();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('EACCES');
  } else {
    return v;
  }
}

int get EEXIST {
  final v = get_EEXIST();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('EEXIST');
  } else {
    return v;
  }
}

int get EINTR {
  final v = get_EINTR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('EINTR');
  } else {
    return v;
  }
}

int get ENOENT {
  final v = get_ENOENT();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('ENOENT');
  } else {
    return v;
  }
}

int get EPERM {
  final v = get_EPERM();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('EPERM');
  } else {
    return v;
  }
}

int get O_APPEND {
  final v = get_O_APPEND();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_APPEND');
  } else {
    return v;
  }
}

int get O_CLOEXEC {
  final v = get_O_CLOEXEC();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_CLOEXEC');
  } else {
    return v;
  }
}

int get O_CREAT {
  final v = get_O_CREAT();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_CREAT');
  } else {
    return v;
  }
}

int get O_EXCL {
  final v = get_O_EXCL();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_EXCL');
  } else {
    return v;
  }
}

int get O_RDONLY {
  final v = get_O_RDONLY();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_RDONLY');
  } else {
    return v;
  }
}

int get O_TRUNC {
  final v = get_O_TRUNC();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_TRUNC');
  } else {
    return v;
  }
}

int get O_WRONLY {
  final v = get_O_WRONLY();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('O_WRONLY');
  } else {
    return v;
  }
}

int get AT_FDCWD {
  final v = get_AT_FDCWD();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('AT_FDCWD');
  } else {
    return v;
  }
}

int get AT_REMOVEDIR {
  final v = get_AT_REMOVEDIR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('AT_REMOVEDIR');
  } else {
    return v;
  }
}

int get PATH_MAX {
  final v = get_PATH_MAX();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('PATH_MAX');
  } else {
    return v;
  }
}

int get S_IEXEC {
  final v = get_S_IEXEC();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IEXEC');
  } else {
    return v;
  }
}

int get S_IFBLK {
  final v = get_S_IFBLK();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFBLK');
  } else {
    return v;
  }
}

int get S_IFCHR {
  final v = get_S_IFCHR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFCHR');
  } else {
    return v;
  }
}

int get S_IFDIR {
  final v = get_S_IFDIR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFDIR');
  } else {
    return v;
  }
}

int get S_IFIFO {
  final v = get_S_IFIFO();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFIFO');
  } else {
    return v;
  }
}

int get S_IFLNK {
  final v = get_S_IFLNK();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFLNK');
  } else {
    return v;
  }
}

int get S_IFMT {
  final v = get_S_IFMT();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFMT');
  } else {
    return v;
  }
}

int get S_IFREG {
  final v = get_S_IFREG();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFREG');
  } else {
    return v;
  }
}

int get S_IFSOCK {
  final v = get_S_IFSOCK();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFSOCK');
  } else {
    return v;
  }
}

int get S_IFWHT {
  final v = get_S_IFWHT();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IFWHT');
  } else {
    return v;
  }
}

int get S_IREAD {
  final v = get_S_IREAD();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IREAD');
  } else {
    return v;
  }
}

int get S_IRGRP {
  final v = get_S_IRGRP();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IRGRP');
  } else {
    return v;
  }
}

int get S_IROTH {
  final v = get_S_IROTH();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IROTH');
  } else {
    return v;
  }
}

int get S_IRUSR {
  final v = get_S_IRUSR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IRUSR');
  } else {
    return v;
  }
}

int get S_IRWXG {
  final v = get_S_IRWXG();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IRWXG');
  } else {
    return v;
  }
}

int get S_IRWXO {
  final v = get_S_IRWXO();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IRWXO');
  } else {
    return v;
  }
}

int get S_IRWXU {
  final v = get_S_IRWXU();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IRWXU');
  } else {
    return v;
  }
}

int get S_ISGID {
  final v = get_S_ISGID();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_ISGID');
  } else {
    return v;
  }
}

int get S_ISTXT {
  final v = get_S_ISTXT();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_ISTXT');
  } else {
    return v;
  }
}

int get S_ISUID {
  final v = get_S_ISUID();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_ISUID');
  } else {
    return v;
  }
}

int get S_ISVTX {
  final v = get_S_ISVTX();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_ISVTX');
  } else {
    return v;
  }
}

int get S_IWGRP {
  final v = get_S_IWGRP();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IWGRP');
  } else {
    return v;
  }
}

int get S_IWOTH {
  final v = get_S_IWOTH();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IWOTH');
  } else {
    return v;
  }
}

int get S_IWRITE {
  final v = get_S_IWRITE();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IWRITE');
  } else {
    return v;
  }
}

int get S_IWUSR {
  final v = get_S_IWUSR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IWUSR');
  } else {
    return v;
  }
}

int get S_IXGRP {
  final v = get_S_IXGRP();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IXGRP');
  } else {
    return v;
  }
}

int get S_IXOTH {
  final v = get_S_IXOTH();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IXOTH');
  } else {
    return v;
  }
}

int get S_IXUSR {
  final v = get_S_IXUSR();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('S_IXUSR');
  } else {
    return v;
  }
}

int get UF_APPEND {
  final v = get_UF_APPEND();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('UF_APPEND');
  } else {
    return v;
  }
}

int get UF_HIDDEN {
  final v = get_UF_HIDDEN();
  if (v == libc_shim_UNDEFINED) {
    throw UnsupportedError('UF_HIDDEN');
  } else {
    return v;
  }
}

